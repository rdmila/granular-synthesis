# Granular Synthesis

## How to build

```sh
cmake -B build
cmake --build build --target gui
```

## How to run

Specify an input file as the command-line argument:
```sh
build/gui <input.wav>
```

(You can try it on `sample2.wav`.)
